{% raw %}
# Railway Frontend Service Environment Variables
# Set these in your Railway dashboard for the frontend service.
#
# Variables using ${{ }} syntax reference other Railway services/variables.
# They are resolved automatically by Railway at deploy time.

REACT_APP_API_URL="https://${{RAILWAY_PUBLIC_DOMAIN}}"
RAILWAY_DOCKERFILE_PATH="frontend/Dockerfile"
BACKEND_URL="${{backend.RAILWAY_PRIVATE_DOMAIN}}"
{% endraw %}
